name: Elan Bridge Runtime Manifest

on:
  push:
    branches:
      - elan-vps-bridge-control-v1

permissions:
  contents: write

jobs:
  publish-manifest:
    if: false
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
